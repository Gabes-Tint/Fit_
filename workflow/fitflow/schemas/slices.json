{
	"type": "object",
	"additionalProperties": false,
	"required": ["spans_domain_and_ui", "slices"],
	"properties": {
		"spans_domain_and_ui": {
			"type": "boolean",
			"description": "True only when work spans both UI and non-UI domain categories."
		},
		"slices": {
			"type": "array",
			"minItems": 1,
			"maxItems": 2,
			"items": {
				"type": "object",
				"additionalProperties": false,
				"required": ["layer", "title", "brief", "acceptance", "test_kind"],
				"properties": {
					"layer": {
						"type": "string",
						"enum": ["domain", "ui", "workflow"],
						"description": "ui is Svelte interface work; domain is every non-UI change to the product, including backend and database work; workflow is a change to the driver itself under workflow/ and the docs that describe it."
					},
					"title": { "type": "string" },
					"brief": { "type": "string" },
					"acceptance": { "type": "array", "items": { "type": "string" } },
					"test_kind": { "type": "string", "enum": ["vitest", "playwright", "pytest"] }
				}
			}
		}
	}
}
